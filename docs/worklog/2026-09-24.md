# Worklog — 2026-09-24

ลงมือตาม `docs/plan/todo.md` ข้อ 1 (next-steps **6.8**) · เจ้าของเลือกทาง **B — pseudonymize**

**สถานะ:** หัวข้อ 1–2 commit แล้ว `6e2d0d5` (push · CI เขียวครบ 5 job + Security Scan) · หัวข้อ 3 ยังไม่ commit (เอกสารอย่างเดียว)

---

## 1. 6.8 — PDPA erasure: pseudonymize แทนการลบ (review B10 · PCI 10.5.1)

### ปัญหา
`DELETE /api/v1/erasure/user/:userId` **ลบ** แถว `audit_access` ของ user · PCI DSS 10.5.1 ให้เก็บ audit trail ≥ 12 เดือน

### ที่แก้
- `src/erasure/erasure.service.ts` — ใน transaction เดียวกับ tombstone:
  - แถวของ user: `user_id` → `anon-<HMAC-SHA256(key, userId)>` · `username` / `ip_address` → NULL
    (`ip_address` เป็น `inet` ใส่ hash ไม่ได้ และ IP ระบุตัวคนได้ → NULL)
  - แถวอื่นที่มี userId ใน `resource` (admin แก้ role ของ user นี้ · URL ของคำขอลบที่เคยทำ) → แทนด้วย HMAC ตัวเดียวกัน
    จับเฉพาะตอนเป็น segment เต็มของ URL (`resourcePattern()` — `(^|[/=])id(?=[/?&#]|$)`) ไม่งั้น id สั้น ๆ แก้ทุกแถว ·
    `left(…, 256)` กันล้นความยาวคอลัมน์ (pseudonym 69 ตัวยาวกว่า UUID)
  - 404 ดูจากแถวของ user เองเท่านั้น (ดูหัวข้อ "เจอระหว่างทดสอบ")
  - ไม่มี key → **503** ไม่ถอยไปลบจริง (ขัด PCI) หรือ hash เปล่า (UUID ของ Keycloak ไล่ hash ย้อนได้)
  - userId ขึ้นต้น `anon-` → 400
- key: `infra/vault/init.sh` สุ่มด้วย `sys/tools/random/32` ของ Vault **ครั้งแรกครั้งเดียว** → `secret/logchain/erasure pseudonym_key`
  (ไม่ได้มาจาก `.env` — seed ใหม่ทุกรอบแล้ว pseudonym ของคำขอเก่ากับใหม่จะไม่ตรงกัน) · `nestjs-policy.hcl` ให้อ่าน ·
  `VaultService.readPseudonymKey()` ไม่มี = warn + erasure ใช้ไม่ได้ ไม่ใช่ boot ไม่ขึ้น
- URL ของคำขอลบเอง: `ErasureController` ตั้ง `req.auditResource` (id → pseudonym) · `AuditInterceptor` ใช้ `req.auditResource ?? req.url`
- migration `ErasurePseudonymize1790467200000` — `erasure_log` เพิ่ม `method` (DEFAULT `'DELETE'` = แถวเก่า) + `pseudonym`
  · หน้า Reports / compliance API คืนสองฟิลด์นี้เพิ่ม · `records_deleted` = จำนวนแถวที่ถูก pseudonymize สำหรับ method ใหม่
- ผู้ถือ key ตามรอยได้: `HMAC(key, userId)` แล้ว `select … from audit_access where user_id = 'anon-…'` — ตั้งใจ (สืบสวนตาม PCI 10)

### เจอเพิ่ม: retention ลบ audit ที่ 90 วัน
`RetentionService` ใช้ `RETENTION_DAYS ?? 90` ทั้ง `alerts` และ `audit_access` · compose **ไม่ส่ง** `RETENTION_DAYS` เข้า backend
→ ใช้ 90 จริง ขณะที่ PCI E05 / 10.2 / ISO R06 เขียน 365 · แก้: default 365 · `audit_access` ใช้ `max(RETENTION_DAYS, 365)` เสมอ
(`retentionDays()` + `retention.service.spec.ts`) · `.env.example` 90 → 365

### เจอระหว่างทดสอบ
- e2e เดิม "404 for unknown user" ได้ 200 — audit ของคำขอ 404 รอบก่อน ๆ เก็บ URL `/erasure/user/unknown-user-id` ไว้
  ถ้านับ reference เป็น "เจอ" จะออก tombstone ให้ id ที่ไม่เคยเป็น user → เปลี่ยนเป็นดูแถวของ user เอง (ไม่เจอ = rollback ทั้งหมด)
  · tombstone ขยะ 1 แถวจากรอบนั้นลบทิ้งแล้ว (ปิด trigger ชั่วคราวแบบเดียวกับ afterAll ของเทสต์)
- Postgres ARE รองรับ lookahead ใน `resourcePattern()` จริง (e2e: `/users/<id>/roles` ถูกแทน · `/users/<id>0/roles` ไม่ถูกแตะ)
- e2e `Alerts Integration › creates an alert` พัง 1 ครั้งแล้วผ่าน 2 รอบติด — ไม่เกี่ยวกับงานนี้ ถ้าเจออีกค่อยดู

### ผลตรวจ
- `npm run lint:ci` 666 warning (เพดาน 667) · `npm test` 231/231 · `npm run build` ผ่าน · e2e 22/22
- บน stack: `vault-init` → `Generating erasure pseudonym key...` · backend → `Vault secrets loaded (…, erasure)` · migration รันแล้ว

### เอกสาร
PCI-SAQ-Evidence 10.2 / E05 / E06 · ISO27001 R05 / A.18.1 · compliance-review (B10 แก้แล้ว) · next-steps 6.8 · todo ข้อ 1

## 2. อุบัติเหตุ: stack ปนกันระหว่างโฟลเดอร์หลักกับโฟลเดอร์ clone

ตอนเริ่ม session container `logchain-*` มาจาก 2 โฟลเดอร์ — `~/Documents/clone_logchain/logchain` (ทดสอบ clone จาก GitHub:
vault-unseal, postgres, kafka, keycloak, …) และ `~/Documents/logchain` (backend, detection, dashboard) · ชื่อ project + volume เดียวกัน
`docker compose up vault-init` จากโฟลเดอร์หลัก recreate `logchain-vault` → sealed · `vault-unseal` ที่รันอยู่เป็นของ clone
(`.secrets/` ว่าง) → "unseal key หาย" วนรีสตาร์ท
แก้: `docker compose up -d` จากโฟลเดอร์หลัก (เจ้าของอนุญาตให้ปิด stack ของ clone) — volume เดิม ข้อมูลไม่หาย · Vault unseal ด้วย `init.env` เดิม
บทเรียน (ใส่ตารางกับดักใน next-steps แล้ว): ทดสอบ clone ต้องใช้ `COMPOSE_PROJECT_NAME` อื่นและปิด stack หลักก่อน

## 3. todo ข้อ 2 — password ของ rclone crypt อยู่นอกเครื่องแล้ว

เจ้าของเก็บ password 2 ตัวใน password manager (แนะนำ: ไม่ใช่ของ Google — บัญชีเดียวกับ Drive ที่เก็บ backup) · ดึงค่าจาก `rclone.conf`
ด้วย `rclone reveal` (ค่าใน conf แค่ obscure ไม่ได้เข้ารหัส) · ทดสอบ: พิมพ์ค่าจาก password manager เข้า crypt remote ชั่วคราว
ใน `logchain-backup-offsite` (ใช้ token ของ `[gdrive]` เดิม · ลบ config ชั่วคราวทิ้ง) → `lsf` ได้ `20260923T151851Z/` `20260923T151852Z/`
(password ผิด = ถอดชื่อไม่ได้ ไม่แสดงอะไร) · ขั้นตอนทั้งหมดรันใน terminal ของเจ้าของเอง password ไม่ผ่านแชต

## 4. todo ข้อ 3 — dashboard ใช้ `next/font/local` แทน Google Fonts

เดิม `next/font/google` (Manrope + JetBrains Mono) ดึงจาก Google Fonts ทุกครั้งที่ build → CI แดงชั่วคราว (worklog 09-23 หัวข้อ 38)
และ `bootstrap.sh` บนเครื่องไม่มีเน็ต build dashboard ไม่ผ่าน
- ไฟล์ variable font ชุด latin (ชุดเดียวกับที่ตั้งไว้เดิม) จาก `@fontsource-variable/{manrope,jetbrains-mono}` 5.3.0 →
  `cylis-dashboard/src/app/fonts/*.woff2` (25 KB + 40 KB) + ไฟล์ license OFL-1.1 · `layout.tsx` ใช้ `localFont` · weight เป็นช่วง
  (`200 800` / `100 800`) ครอบ 400–800 / 400–600 ที่ใช้อยู่ · CSS variable `--font-sans` / `--font-mono` เหมือนเดิม
- ตรวจ: `tsc --noEmit` + `next build` ใน `node:20 --network none` ผ่าน · container ใหม่เสิร์ฟ woff2 จาก `/_next/static/media/` (200 `font/woff2`)
  · HTML ไม่มี `googleapis` / `gstatic`

## 5. todo ข้อ 3 — `/metrics` ของ backend ไม่เปิดสาธารณะแล้ว (review B5)

เดิม `GET /metrics` อยู่บน `:3000` เดียวกับ API ไม่มี auth → เห็นจำนวน batch ตามสถานะ / อายุ batch ค้าง ถ้าตั้ง `PUBLISH_ADDR=0.0.0.0` ก็ทั้ง LAN
- เลือก **port แยกที่ไม่ publish** (`src/metrics/metrics-server.ts` · `:9464` · `METRICS_PORT`) แทน bearer token — Prometheus ไม่ต้อง
  ถือ secret (config ของ Prometheus อ่าน env ไม่ได้ ต้องทำไฟล์ + render แบบ alertmanager) · แบบเดียวกับ detection-consumer `:9101`
- ลบ `MetricsController` · `main.ts` เรียก `startMetricsServer()` · `AuditInterceptor` เลิกข้าม `/metrics` (ใครยิง `:3000/metrics` = 404 + ถูกบันทึก)
- `prometheus.yml` target `backend:9464` · `alerts.test.yml` label `instance` ตาม · README / PCI 8.1 + E01 / ISO A.9.1 / review B5
- ตรวจบน stack: `:3000/metrics` → 404 · `:9464` จาก host → ต่อไม่ได้ · Prometheus target `backend:9464` up · alert `ServiceDown`
  ของ target เดิม (`backend:3000`) pending แวบเดียวตอน reload แล้วหายเอง (series stale) ไม่ยิง email
- ยังเหลือ: detection-api `:8000/metrics` (prometheus-fastapi-instrumentator — สถิติ HTTP request อย่างเดียว ความเสี่ยงต่ำ)
- `lint:ci` 664 · `npm test` 234/234 · build · e2e 22/22 · promtool test rules + check config ผ่าน

## 6. todo ข้อ 3 — pip-audit ครอบ dependency ทางอ้อม (review B7)

เดิม Security Scan `pip-audit -r detection/requirements.txt --no-deps` = ตรวจ 15 ตัวที่ pin · ใน image มีจริง **48 ตัว**
(starlette, urllib3, jinja2, aiohttp, … ไม่ถูกตรวจ) และรุ่นของตัวทางอ้อมขึ้นกับวันที่ build
- `detection/requirements.lock` = `pip freeze --all` ของ image ที่รันอยู่ (torch เขียน `2.13.0` ตัด `+cpu`) · audit ทั้ง 48 ตัว: ไม่มีช่องโหว่
- Dockerfile: `COPY requirements.txt requirements.lock` · pip/setuptools ลงตาม lock · torch CPU wheel `--no-deps` (dependency ของ torch
  ไปลงจาก PyPI ตาม lock แทนรุ่นใดก็ได้จาก index ของ pytorch) · `pip install -r requirements.txt -c requirements.lock && pip check`
  แล้ว **เทียบ `pip freeze` กับ lock — ไม่ตรง build พัง** (`-c` บังคับได้แค่ตัวที่อยู่ใน lock)
- `scripts/detection-lock.sh` — ทำขั้นเดียวกับ Dockerfile แบบไม่มี lock ใน `python:<ver>-slim` → freeze → lock ใหม่ → pip-audit
- `security.yml` audit `requirements.lock` แทน `requirements.txt`
- ตรวจ: build ปกติผ่าน + `pip check` · freeze ของ image ใหม่ == lock · context ทดสอบที่เพิ่ม `six` ใน requirements.txt → build พัง
  พร้อม diff `+six==1.17.0` · รัน script ได้ lock 48 ตัว + audit ผ่าน (รอบนั้นได้ filelock 4.0.3 / fsspec 2026.9.0 / networkx 3.7
  ที่ใหม่กว่า — **คืน lock เดิม** ให้ตรงกับ image ที่ทดสอบแล้ว ไม่พ่วงการอัปเกรดมากับงานนี้) · Python unittest 7/7 ·
  brute force demo → `DETECTED [RULE_MATCH]` → alert เข้า DB

## 7. todo ข้อ 3 — private key ของ Kafka ไม่เปิดให้ทุกคนอ่าน (+ backend อ่าน `ca.key` ได้)

เดิม `gen-certs.sh` ตั้ง key ทุกตัว 0644 เพราะ uid ใน container (detection 10001 · bitnami kafka 1001) ไม่ตรงเจ้าของไฟล์
**เจอหนักกว่า:** backend mount ทั้ง `infra/kafka/certs/` ซึ่งมี `ca.key` — backend รัน uid 1000 = เจ้าของไฟล์บนเครื่อง dev → อ่าน
private key ของ CA ได้ (โดนเจาะ backend = ออก client cert เข้า Kafka เองได้) และเห็น key ของ detection ด้วย
- `gen-certs.sh`: cert 0644 · key 0640 · `ca.key` 0600 · `bootstrap.sh` บีบสิทธิ์ของ cert ที่มีอยู่แล้วให้ตรงทุกรอบ
- compose: `group_add: ["${HOST_GID:-1000}"]` ให้ kafka-1..3 / backend / detection-consumer — อ่าน key ผ่านกลุ่มเจ้าของไฟล์
- backend / detection-consumer mount เฉพาะ `ca.crt` + cert + key ของตัวเอง (file bind) · `KAFKA_SSL_CA` ของ backend → `clients/ca.crt`
- ตรวจ: recreate broker ทีละตัว healthy · log ไม่มี SSL/permission error · backend `ls` เห็น 3 ไฟล์ ไม่มี `ca.key` ·
  detection (uid 10001 + กลุ่ม 1000) อ่าน key ได้ · brute force demo → DETECTED → alert เข้า DB · `kafka_pending_logs` 0 ·
  `demo-mtls.sh` ผ่าน · รันสำเนา `gen-certs.sh` ใน scratchpad → สิทธิ์ตามที่ตั้ง
- ข้อควรระวัง: ต้อง `HOST_GID=$(id -g)` ทุกครั้ง (กฎเดิมอยู่แล้ว) — เครื่องที่ gid ไม่ใช่ 1000 ลืมใส่ = Kafka SSL ขึ้นไม่ได้ (ใส่ตารางกับดักแล้ว)
