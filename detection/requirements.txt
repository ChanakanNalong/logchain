# เวอร์ชันทั้งหมดอ่านจาก venv ที่ใช้งานจริง (2026-09-21)
#
# torch: ค่า default จาก PyPI เป็น build ที่ลาก nvidia CUDA libs มา ~3 GB
# inference บน HDFS sequence ไม่ต้องใช้ GPU — Dockerfile เลยลง CPU wheel
# จาก https://download.pytorch.org/whl/cpu ก่อน แล้วบรรทัดนี้จะถือว่า satisfied
# (2.12.0+cpu เข้าเงื่อนไข ==2.12.0 ตาม PEP 440 — local version segment ถูกมองข้าม)
# รันเองบน host ที่อยากได้ CPU wheel เหมือนกัน:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.12.0
#   pip install -r requirements.txt
torch==2.12.0

fastapi==0.137.2
uvicorn==0.49.0
pydantic==2.13.4
prometheus-fastapi-instrumentator==8.0.2

kafka-python==3.0.11
drain3==0.9.11
geoip2==5.2.0
hvac==2.4.0
numpy==2.4.6
PyYAML==6.0.3
requests==2.34.2
python-dotenv==1.2.2
tqdm==4.68.2
